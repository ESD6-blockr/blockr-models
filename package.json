{
  "name": "blockr-models",
  "version": "1.0.0",
  "description": "Blockr models package",
  "main": "",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "build-watch": "tsc -w -p tsconfig.json",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "tslint --project tsconfig.json"
  
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ESD6-blockr/blockr-models.git"
  },
  "author": "Blockr",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "tslint": "^5.14.0",
    "tsutils": "^3.9.1",
    "typescript": "^3.3.4000"
  }
}
