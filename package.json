{
  "name": "@blockr/blockr-models",
  "version": "1.4.0",
  "description": "Blockr models package",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json && cpy src/*.proto dist/",
    "build-watch": "tsc -w -p tsconfig.json",
    "lint": "tslint --project tsconfig.json",
    "test": "jest || exit 0",
    "dev": "npm run lint && npm run build"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/ESD6-blockr/blockr-models.git"
  },
  "author": "Blockr",
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^24.0.13",
    "jest": "^24.8.0",
    "jest-junit": "^6.4.0",
    "ts-jest": "^24.0.2",
    "tslint": "^5.15.0",
    "tsutils": "^3.10.0",
    "typescript": "^3.4.3",
    "cpy-cli": "^2.0.0"
  }
}
